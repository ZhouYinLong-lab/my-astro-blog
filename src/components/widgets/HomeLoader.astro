---
// 全新响应式 Loading 组件
---

<div class="loader-wrapper" id="home-loader">
  <div class="loader-content">
    
    {/* 上方：头像与轨道核心区 */}
    <div class="avatar-core">
      {/* 两个反向旋转的能量环 */}
      <div class="orbit orbit-1"></div>
      <div class="orbit orbit-2"></div>
      {/* 居中静止的专属头像 */}
      <img src="/favicon.ico" alt="Loading" class="core-avatar" />
    </div>

    {/* 下方：波浪跳动文字区 */}
    <div class="text-core">
      <span>L</span><span>O</span><span>A</span><span>D</span><span>I</span><span>N</span><span>G</span>
      <span class="dot">.</span><span class="dot">.</span><span class="dot">.</span>
    </div>

  </div>
</div>

<style>
  /* 基础与背景层 */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  .loader-wrapper {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    justify-content: center;
    align-items: center;
    /* 保持你喜欢的渐变背景 */
    background: linear-gradient(45deg,rgb(90,54,148) 0%,rgb(19,189,206) 33%,rgb(0,148,217) 66%,rgb(111,199,181) 100%);
    background-size: 400%;
    animation: gradientBG 7.5s ease-in-out infinite;
    transition: opacity 0.8s ease-out, visibility 0.8s ease-out;
  }

  .loader-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 30px; /* 控制头像和文字的间距 */
  }

  /* ================= 能量环与头像区 ================= */
  .avatar-core {
    position: relative;
    width: 100px;
    height: 100px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .core-avatar {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    object-fit: cover;
    z-index: 10;
    border: 2px solid rgba(255, 255, 255, 0.9);
    box-shadow: 0 0 15px rgba(255, 255, 255, 0.3);
  }

  .orbit {
    position: absolute;
    border-radius: 50%;
    border: 3px solid transparent;
  }

  /* 内圈：顺时针快转 */
  .orbit-1 {
    width: 86px;
    height: 86px;
    border-top-color: #fff;
    border-right-color: #fff;
    animation: spin 1.2s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
  }

  /* 外圈：逆时针慢转带透明度 */
  .orbit-2 {
    width: 104px;
    height: 104px;
    border-bottom-color: rgba(255, 255, 255, 0.6);
    border-left-color: rgba(255, 255, 255, 0.6);
    animation: spin 2s linear infinite reverse;
  }

  /* ================= 文字波浪跳动区 ================= */
  .text-core {
    display: flex;
    gap: 8px; /* Flexbox 的 gap 属性完美解决手机端重叠问题 */
    font-size: 22px;
    font-weight: 800;
    color: #fff;
    letter-spacing: 2px;
    user-select: none;
    -webkit-user-select: none;
  }

  .text-core span {
    display: inline-block;
    animation: waveJump 1.5s ease-in-out infinite;
  }

  /* 为每个字母设置依次递增的延迟，形成波浪效果 */
  .text-core span:nth-child(1) { animation-delay: 0.1s; }
  .text-core span:nth-child(2) { animation-delay: 0.2s; }
  .text-core span:nth-child(3) { animation-delay: 0.3s; }
  .text-core span:nth-child(4) { animation-delay: 0.4s; }
  .text-core span:nth-child(5) { animation-delay: 0.5s; }
  .text-core span:nth-child(6) { animation-delay: 0.6s; }
  .text-core span:nth-child(7) { animation-delay: 0.7s; }
  .text-core span:nth-child(8) { animation-delay: 0.8s; }
  .text-core span:nth-child(9) { animation-delay: 0.9s; }
  .text-core span:nth-child(10) { animation-delay: 1.0s; }

  /* ================= 关键帧动画 ================= */
  @keyframes gradientBG {
    50% { background-position: 100% 0%; }
  }

  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  @keyframes waveJump {
    0%, 100% { transform: translateY(0); opacity: 0.4; }
    50% { transform: translateY(-8px); opacity: 1; }
  }
</style>

<script is:inline>
  document.addEventListener("astro:page-load", () => {
    const loader = document.getElementById("home-loader");
    if (loader) {
      setTimeout(() => {
        loader.style.opacity = "0";
        loader.style.visibility = "hidden";
      }, 1200); // 留出一点时间让访客欣赏这个绝美的开场
    }
  });
</script>